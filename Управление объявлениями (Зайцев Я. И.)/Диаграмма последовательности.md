```plantuml
@startuml
actor Пользователь

participant UI
participant MessageBoardFacade
participant "IAdvertisementsRepository" as IRepository
participant "AdvertisementsRepository" as Repository
participant Database
participant "NotificationService" as Notification

Пользователь-> UI: Выбирает "Мои объявления"
activate UI

UI -> MessageBoardFacade: GetUserAdvertisements(userId: uInt32)
activate MessageBoardFacade

MessageBoardFacade -> IRepository: FindByUserId(userId: uInt32)
activate IRepository

IRepository -> Repository: FindByUserId(userId: uInt32)
activate Repository

Repository -> Database: SELECT * WHERE Id = userId
activate Database 

Database -> Repository: Advertisments

deactivate Database 
Repository -> IRepository: List<Advertisment>

deactivate Repository
IRepository -> MessageBoardFacade: List<Advertisment>

deactivate IRepository
MessageBoardFacade -> UI: List<Advertisment>
deactivate MessageBoardFacade
UI --> Пользователь: Отображаются объявления пользователя

deactivate UI
Пользователь -> UI: Нажимает "Добавить объявление"
activate UI

UI --> Пользователь: Показывает форму создания

deactivate UI
Пользователь-> UI: Заполняет и сохраняет объявление
activate UI

UI -> MessageBoardFacade: CreateAdvertisement(userId: uInt32, advertisementDTO)
activate MessageBoardFacade

MessageBoardFacade -> IRepository: Create(userId: uInt32,advertisement: Advertisement)
activate IRepository 

IRepository -> Repository: Create(userId: uInt32,advertisement: Advertisement)
activate Repository 

Repository -> Database: INSERT INTO advertisements (VALUES)
activate Database 

Database --> Repository: Advertisment

deactivate Database 
Repository --> IRepository: Advertisment

deactivate Repository 
IRepository --> MessageBoardFacade: Advertisment

deactivate IRepository 
MessageBoardFacade -> Notification: notifyModerationStatus(advertisementId: uInt32, status: String)

MessageBoardFacade -> IRepository: FindByUserId(userId: uInt32)
activate IRepository

IRepository -> Repository: FindByUserId(userId: uInt32)
activate Repository

Repository -> Database: SELECT * WHERE Id = userId
activate Database

Database -> Repository: Advertisments (Обновленные данные)

deactivate Database
Repository -> IRepository: List<Advertisment> (Обновленный список)

deactivate Repository
IRepository -> MessageBoardFacade: List<Advertisment> (Обновленный список)

deactivate IRepository
MessageBoardFacade -> UI: List<Advertisment> (Обновленный список)

deactivate MessageBoardFacade
UI --> Пользователь: Показывает обновленный список объявлений

deactivate UI
@enduml
@enduml```